---
applyTo: "**/*"
description: "Template editing patterns and placeholder conventions."
---

# Patterns

Follow the template patterns and conventions defined in [.claude/rules-snippets/patterns.md](../rules-snippets/patterns.md).

Key rules:
- Use `{{PLACEHOLDER_NAME}}` for all customizable values (SCREAMING_SNAKE_CASE).
- Keep templates generic and framework-agnostic.
- Prefer extending existing utilities over creating duplicates.
